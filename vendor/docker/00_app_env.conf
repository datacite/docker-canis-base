# Common environment passthrough for Passenger workers.
# Apps can override this entire file by copying their own version
# in their Dockerfile after the base image is used.
env RAILS_ENV;
env RACK_ENV;
env SECRET_KEY_BASE;
env DATABASE_URL;
env ELASTICSEARCH_URL;
env AWS_REGION;
